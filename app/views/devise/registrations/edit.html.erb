<div class="container my-5">
  <div class="row">
    <div class="card p-5 col-md-6 offset-md-3">
      <h2>회원정보수정</h2>
      <hr>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true, autocomplete: "이메일", class:"form-control" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>

        <div class="form-group">
          <%= f.label :password %> <i>(비밀번호 변경시에만 입력해주세요)</i>
          <%= f.password_field :password, autocomplete: "비밀번호", class:"form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation %>
          <%= f.password_field :password_confirmation, autocomplete: "비밀번호 확인", class:"form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :current_password %> <i>(정보수정시 입력해주세요)</i>
          <%= f.password_field :current_password, autocomplete: "현재 비밀번호", class:"form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "정보 수정", class:"btn btn-primary" %>
        </div>
      <% end %>
      <hr>
      <h3>회원 탈퇴</h3>

      <p>탈퇴 후엔 모든 정보가 삭제됩니다 <%= button_to "회원 탈퇴", registration_path(resource_name), data: { confirm: "탈퇴 하시겠습니까?" }, method: :delete, class:"btn btn-danger" %></p>

      <%= link_to "뒤로가기", :back, class:"btn btn-light"%>
    </div>
  </div>
</div>